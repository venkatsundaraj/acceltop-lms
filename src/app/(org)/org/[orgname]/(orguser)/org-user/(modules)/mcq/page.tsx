import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <section className="w-full flex items-center justify-center flex-col"></section>
  );
};

export default page;
